{
  "name": "grafos",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile --outfile=out/create-graph.exe ./src/create-graph.ts && bun build --compile --outfile=out/csv-to-dot.exe ./src/csv-to-dot.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
